# Work Log: Sweep effects readout

**Session:** 2026-08-24_1154
**Status:** In progress

Related: [2026-08-24_0215_results_tree_migration.md](2026-08-24_0215_results_tree_migration.md)
(why the requested `data/` paths now resolve under `results/`).

---

## Original Request

> Read data/detector_tuning/sweep_results.csv and
> data/detector_tuning/sweep_results_min_area_circ.csv. Do not modify either.
> Report: (a) for the stride/threshold/open-kernel sweep, the top config by raw
> avg_combined_rate with its labelled recall, the baseline recall used as the
> gate, the number of configs passing the gate out of the total, and the
> gate-passing winner's config and both metrics; (b) for the min_area/min_circ
> sweep, the baseline and winning config with both metrics, confirming min_circ
> is held at 0.30 across the compared pair, and the number of grid combinations
> failing the recall gate. Every number must print with its CSV path and row
> locator. STOP and report NOT_FOUND rather than substituting if either sweep CSV
> lacks a labelled-recall column. Write to data/regenerate_figures/sweep_effects.txt.
> Log incrementally.

---

## [11:54] Step 1 - path resolution and the STOP condition

### Paths

Neither requested path exists as written:

- `data/detector_tuning/sweep_results.csv` - **missing**
- `data/detector_tuning/sweep_results_min_area_circ.csv` - **missing**

Both were moved to `results/detector_tuning/` by the 2026-08-24_0215 migration
(which moved everything under `data/detector_tuning/` EXCEPT `contact_sheets/`).
Same files, new location - not a substitution of different data, so proceeding
rather than reporting NOT_FOUND (that gate is specifically about a missing
labelled-recall column).

- `results/detector_tuning/sweep_results.csv` - 49 lines
- `results/detector_tuning/sweep_results_min_area_circ.csv` - 25 lines

Output path `data/regenerate_figures/` likewise does not exist;
`results/regenerate_figures/` does, same migration. Writing there.
`sweep_effects.txt` does not exist yet - no overwrite.

### STOP condition: labelled-recall column present in BOTH

- `results/detector_tuning/sweep_results.csv` line 1:
  `stride,diff_threshold,open_kernel,avg_combined_rate,labeled_recall,meets_recall_gate,is_baseline`
- `results/detector_tuning/sweep_results_min_area_circ.csv` line 1:
  `min_area,min_circ,avg_combined_rate,labeled_recall,meets_recall_gate,is_baseline`

Both carry `labeled_recall`. **STOP does not fire.** Continuing.

### Flag carried into the analysis

`claude/decision_log.md` #15 records that `sweep_results.csv` once carried
`meets_recall_gate`/`is_baseline` columns that its own generating script did not
actually compute. So the stored `meets_recall_gate` column is NOT trusted as-is -
it will be recomputed from `labeled_recall` vs the baseline row and the two
compared before any gate count is reported.

## [11:56] Step 2 - both sweeps read, script written, output emitted

`src/regen_2class/sweep_effects.py`. Row locators are 1-based file line numbers
(header = line 1), so every figure traces to an exact CSV row.

### Gate integrity check (decision_log #15)

Recomputed the gate as `labeled_recall >= baseline labeled_recall` and compared
against the stored `meets_recall_gate` column:

- sweep_results.csv: **matches on all 48 rows**
- sweep_results_min_area_circ.csv: **matches on all 24 rows**

So #15's concern does not materialise for these two files - the stored column is
consistent with the data. Reported numbers use the recomputed gate regardless.

### (a) stride x diff_threshold x open_kernel - 48 configs, lines 2-49

| | line | config | rate | recall |
|---|---|---|---|---|
| baseline (`is_baseline=True`) | 7 | s=1 t=20 k=7 | 0.2772 | **0.9074** (gate) |
| top by RAW rate | 8 | s=2 t=8 k=3 | **1.0000** | 0.5556 - FAILS |
| gate-passing winner | 2 | s=1 t=16 k=3 | 0.8740 | 0.9259 |

**6 of 48 pass the gate** (lines 2-7 - every one of them stride=1).

The raw winner is the documented "combined_rate ranking is gameable" case: a
perfect 1.0000 combined rate with recall 0.3519 *below* the baseline gate.

Winner vs baseline: rate 0.2772 -> 0.8740 (**3.15x**), recall 0.9074 -> 0.9259.

### (b) min_area x min_circ - 24 combinations, lines 2-25

| | line | config | rate | recall |
|---|---|---|---|---|
| baseline | 22 | min_area=200 min_circ=0.3 | 0.8552 | **0.8125** (gate) |
| adopted | 6 | min_area=30 min_circ=0.3 | 0.9751 | 0.9208 |

Deltas: rate +0.1199, recall +0.1083.

**min_circ = 0.30 CONFIRMED in both rows** (line 22 and line 6) - the compared
pair differs only in min_area, 200 -> 30.

**2 of 24 fail the gate**: line 24 (200, 0.25) recall 0.8000, line 25 (200, 0.2)
recall 0.7958. Both are min_area=200 rows at looser circularity.

### Discrepancy found - flagged, not silently resolved

`claude/decision_log.md` describes the adopted config as taking "the highest
recall in the grid (0.9208)". **0.9208 is not the grid maximum.** Two rows carry
recall 0.9250: line 10 (min_area=30, min_circ=0.35, rate 0.9580) and line 12
(min_area=50, min_circ=0.35, rate 0.9547).

0.9208 is the maximum only among min_circ <= 0.30 rows. The decision itself is
unaffected - the 0.9825-vs-0.9751 rate trade the log describes is the pair
(30,0.25) vs (30,0.30), both at min_circ <= 0.30 - but a report quoting "highest
recall in the grid" would be wrong. Recorded in the output file as a NOTE.

### Source files untouched

Confirmed by mtime after the run: `sweep_results.csv` 2026-07-23 13:00,
`sweep_results_min_area_circ.csv` 2026-07-24 10:07. Both unchanged; opened
read-only.

## Deliverable

`results/regenerate_figures/sweep_effects.txt` (new file, 4502 bytes - did not
exist before, nothing overwritten).

**Status: Complete.**
