READ FIRST: dev/claude_rules.md

═══════════════════════════════════════════════════════════════════════════════
TASK
═══════════════════════════════════════════════════════════════════════════════

Regenerate the timing, convergence and duration figures under a two-class scheme
(SHORT / LONG at a 45 degree elevation cut), and produce a new per-flight outcome
sweep that counts success, late, wrong and no-response across the T grid.

CONTEXT: the existing three-class scheme (FLAT/MID/LOB) is being replaced because
the elevation distribution is bimodal and MID (n=12) sits in the sparse valley.
The existing deadlines were not derived by a consistent rule: FLAT 490 is
min-anchored, MID 710 and LOB 1080 are not. This run recomputes both class
deadlines by a single stated rule.

═══════════════════════════════════════════════════════════════════════════════
LOGGING (DETAILED LEVEL)
═══════════════════════════════════════════════════════════════════════════════

Create work log: dev/logs/2026-08-20_[HHMM]_two_class_regen.md
Follow dev/log_template.md. Append in real time after each figure and each
assertion. Print every summary table to the log as well as to CSV.

All outputs (figures and CSVs) go to data/regenerate_figures/

═══════════════════════════════════════════════════════════════════════════════
SCOPE - WHAT TO DO
═══════════════════════════════════════════════════════════════════════════════

STEP 1 - CLASSES
From the bin column in pipeline_sweep_raw.csv:
  SHORT = FLAT union MID    LONG = LOB
Cross-check against elevation_deg in launch_to_crossing.csv. Assert every SHORT
flight has elevation < 45 and every LONG flight >= 45. Report mismatches and STOP
if any are found.
Report n per class. Expected 47 and 60, total 107.

STEP 2 - DEADLINES
Per class, from launch_to_crossing_ms, print: n, min, P5, P25, median, max.
Set deadline = min, rounded DOWN to the nearest 10 ms.
Print the chosen values and state the rule explicitly in the log.
Do NOT hardcode 490, 710 or 1080 anywhere in the script.
Note in the log that this changes the long-class deadline from the previously
used 1080, because that value was not min-anchored.

>>> CHECKPOINT 1: STOP HERE. Report class counts and both deadlines. Wait for
>>> my approval before continuing to Step 3.

STEP 3 - JOIN
pipeline_sweep_raw.csv on (session, flight)
  -> launch_to_crossing.csv on (session, flight_id)
  -> crossing_classification.csv on (session, flight_id), taking duration_ms
NEVER join on flight alone. flight_13 exists in two sessions and joining on the
bare id silently compares the wrong flight.
Note: crossing_classification.csv contains crossing_vel_xyz, a quoted JSON-style
list inside a single field. Use a proper CSV parser, not a naive comma split.
Assert 2568 rows after join, 107 distinct flights, 24 T values.

STEP 4 - FIGURE A: margin vs cutoff
margin_p95(T) = deadline(class) - T - p95(latency_ms over that class at T)
One solid line per class.
One horizontal reference line at -84 ms, labelled:
  "target mode budget (1080p@240Hz: 4ms panel, 4.2ms quantisation, ~8ms render)"
No margin = 0 line. No chaos rally band.
Mark max-usable-T per class with a vertical line and print both values.

STEP 5 - FIGURE B: position error convergence
Median position_error_mm per class per T, IQR shaded.
Exclude status == 'fit_failed' from the median, but print the excluded count per
class per T beneath the plot and in the log.
Caption must state this is CONVERGENCE against the full-arc Model-C fit, NOT
ground truth.
Vertical lines at the max-usable-T values from Figure A.

STEP 6 - FIGURE C: duration distribution, crossers only
Histogram of launch_to_crossing_ms, all 107 flights, two colours by class,
OVERLAID not stacked.
Vertical markers at min/P5/median/max per class.
Title states n=107 crossing flights. Caption states this is launch-to-crossing
time, not observable track length, and that the previous n=158 figure plotted a
different quantity on a different population.
Additionally print: max of SHORT, min of LONG, and the count of SHORT flights
whose duration exceeds LONG's minimum. That count is the confusion region of the
45 degree proxy and is the main thing this figure exists to produce.

STEP 7 - FIGURE D: outcome sweep
Per flight per T:
  t_obs      = min(T, duration_ms)
  answered   = (status == "ok")
  in_time    = (t_obs + latency_ms) <= (launch_to_crossing_ms + 84)
  accurate   = (position_error_mm < 200)
Verdict precedence, first match wins:
  not answered   -> "no_response"
  not in_time    -> "late"
  not accurate   -> "wrong"
  otherwise      -> "success"
Store the three booleans alongside the verdict.
Three stacked-bar panels: SHORT, LONG, POOLED. All 24 T values on the x-axis.
Band order bottom to top: success, wrong, late, no_response.
Assert the four counts sum to the panel's n at every T.
Save CSV: class, T, n_success, n_late, n_wrong, n_no_response, success_rate.
Report the T maximising success_rate for each panel, and at that T report how
often late and wrong co-occur.

All figures 150 dpi.

═══════════════════════════════════════════════════════════════════════════════
SCOPE - WHAT NOT TO DO
═══════════════════════════════════════════════════════════════════════════════

- Do NOT write outside data/regenerate_figures/ and dev/logs/
- Do NOT modify any existing CSV, JSON or script
- Do NOT re-run any Pi benchmark, capture, detection or fitting job
- Do NOT commit to git or create branches
- Do NOT regenerate any figure not listed above
- Do NOT use position_error_mm as an accuracy figure anywhere in a caption. It is
  convergence against the full-arc fit.
- Do NOT use last_pair_detect_ms as a timestamp. It is a compute duration.
- Do NOT hardcode any deadline value
- Do NOT drop fit_failed rows from Figure D. They are the no_response band and
  the denominator is always the class n.

═══════════════════════════════════════════════════════════════════════════════
TIMING EXPECTATIONS
═══════════════════════════════════════════════════════════════════════════════

Steps 1-2: 3-5 minutes, then CHECKPOINT 1
Steps 3-7: 25-35 minutes
Total: 30-40 minutes

STOP and report if:
- Any step takes more than 2x the estimate
- Stuck on the same issue for more than 5 minutes
- Any assertion fails

═══════════════════════════════════════════════════════════════════════════════
CHECKPOINTS
═══════════════════════════════════════════════════════════════════════════════

CHECKPOINT 1 (after Step 2): report class counts and both deadlines, then WAIT.
CHECKPOINT 2 (after Step 6): report Figures A, B, C plus the confusion region
count, then WAIT before running the outcome sweep.

Do not run unsupervised past either checkpoint.

═══════════════════════════════════════════════════════════════════════════════
ERROR HANDLING
═══════════════════════════════════════════════════════════════════════════════

STOP immediately if:
- The elevation cross-check finds any bin/elevation mismatch
- Row count after join is not 2568, or distinct flights is not 107
- Outcome counts do not sum to n at any T
- Any join produces duplicate or missing rows
- duration_ms is missing for any of the 107 flights

Expected and continuable:
- fit_failed rows (87 total across the grid) - these are data, not errors

═══════════════════════════════════════════════════════════════════════════════
GIT WORKFLOW: No git. Do not create branches, do not commit.
═══════════════════════════════════════════════════════════════════════════════

SUCCESS CRITERIA
✅ Class counts 47 / 60 / 107, elevation cross-check passes
✅ Both deadlines computed by the stated min rule, no hardcoded values
✅ Figures A, B, C, D written to data/regenerate_figures/ at 150 dpi
✅ Outcome CSV written, counts sum to n at every T
✅ Best-T reported for SHORT, LONG and POOLED
✅ Confusion region count reported
✅ Log complete with every assertion result

START WORK





Figures A and D only. Replot from existing computed data. Do NOT recompute margins
or verdicts except where item 4 says so. Do NOT re-run the Pi sweep.

Write plotting code to scripts/regen_2class/. No heredocs.

1. FIGURE A BUDGET LINE LABEL
   Replace the current label with:
     "-84 ms  -  target mode budget (4K@60Hz, 16ms input lag, BenQ X500i)"
   The value -84 is unchanged (100 ms perceptual window minus 16 ms input lag).
   Remove the 4ms panel / 4.2ms quantisation / 8ms render decomposition entirely -
   projector input lag specs are measured end-to-end and already include the frame
   period, so those terms double-counted. Add to the caption that Pi render and
   compositor latency is neglected.

2. AXIS LABEL, ALL FIGURES
   Change every x-axis label from "prediction cutoff T (ms)" to
   "observation window (ms)". Same in every annotation, legend and caption. This
   matches the naming in the pipeline timing diagram.

3. FIGURE D PANELS
   Keep all three panels: SHORT, LONG, POOLED. Do not drop pooled.
   Move POOLED to the TOP of the figure, above SHORT and LONG. It is the
   headline: the deployed system has no regime classifier and must use one
   universal cutoff, so pooled is actual performance while the per-class panels
   are the classifier-equipped upper bound.
   Add to the caption: "POOLED is the performance of a system with no regime
   classifier. SHORT and LONG are the achievable performance if the class were
   known at prediction time."

4. FIGURE D SENSITIVITY RUN
   Produce a second version of Figure D with the accuracy test at
   position_error_mm < 170 instead of < 200, everything else identical.
   Rationale to record in the log: position_error_mm is convergence against the
   full-arc Model-C fit, not error against ground truth. Total error against truth
   is approximately the quadrature of convergence and the ~106 mm label-vs-fit
   accuracy floor, so a 200 mm requirement against truth corresponds to roughly
   sqrt(200^2 - 106^2) = 170 mm of allowable convergence error.
   Save as a separate file, do not overwrite the 200 mm version.
   Print a comparison table: for each panel, best T and success rate at 200 mm
   versus at 170 mm.

5. BACKFILL SCRIPTS
   Yes, backfill steps 1-3 and 5 as script files under scripts/regen_2class/ so
   every figure in the report is reproducible from a file rather than from the
   log. Do not re-run anything - just write the scripts and confirm each
   reproduces the values already recorded in the log.

Save to data/regenerate_figures/ at 150 dpi. Log all changes and the 200 vs 170
comparison table.